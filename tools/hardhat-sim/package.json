{
  "name": "zkcex-hardhat-sim",
  "version": "0.1.0",
  "private": true,
  "description": "Local hardhat node + ZkERC20 tokens used by the zkCEX deposit/withdraw demo.",
  "scripts": {
    "node": "hardhat node --hostname 127.0.0.1 --port 8545",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "ethers": "^6.13.2",
    "hardhat": "^2.22.10"
  }
}
